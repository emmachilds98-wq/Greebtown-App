# Ideas for later sessions

Not committed to, not scoped — just parked here so they don't get lost.

## Play preview snippets of artists

Let people hear a clip of an act straight from the Lineup screen (list view, timeline
detail modal) — already built (`js/artist-previews.js` + `artistPreviewBlockHtml()`/
`wirePreviewButtons()` in `js/app.js`), currently focused on expanding coverage and
polish rather than starting from scratch. See session notes for the current approach
(Viberate MCP for verified artist matching, hand-checked before adding an entry) and
options for scaling beyond hand-picked entries to the full lineup.
