// Researched, artist-specific one-line descriptions of sound/style —
// keyed by exact artist name as it appears in js/app.js's `artists`
// array. Populated from real research (Wikipedia, Resident Advisor,
// label/agency bios, official socials), not invented. Acts without an
// entry here fall back to the generic per-genre blurb in GENRE_INFO
// (see artistBioText() in js/app.js).
window.ARTIST_BIOS = {};
